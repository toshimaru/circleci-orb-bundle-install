version: "2.1"

jobs:
  build:
    docker:
      - image: circleci/ruby
    steps:
      - mv test/Gemfile.sample Gemfile
      - bundle install

      # test commnad
      - bundle-install/bundle-install
