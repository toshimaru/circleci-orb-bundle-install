version: "2.1"

jobs:
  build:
    docker:
      - image: circleci/ruby
    steps:
      - run: mv test/Gemfile.sample Gemfile
      - run: bundle install

      # test commnad
      - bundle-install/bundle-install
